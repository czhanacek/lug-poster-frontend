image: node:6


before_script:
  - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client -y )'
  - eval $(ssh-agent -s)
  - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add - > /dev/null
  - npm -v
  - npm install
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh



stages:
  - build
  - deploy

build:
  stage: build
  script:
    - npm build

deploy:
  stage: deploy
  script:
    - rsync -a build/ ci@wsu.party:build/
    